import AgencyPlaceholder from "@/components/agency/AgencyPlaceholder";

export default function ClientsPage() {
  return <AgencyPlaceholder title="Clients" subtitle="Every client, their strategy, and production pipeline." note="Client list + AI channel analysis and per-client detail pages." />;
}
