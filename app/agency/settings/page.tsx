import AgencyPlaceholder from "@/components/agency/AgencyPlaceholder";

export default function SettingsPage() {
  return <AgencyPlaceholder title="Settings" subtitle="Integrations, AI training, and team access." note="API keys, the AI-training engine, and integration setup guides." />;
}
