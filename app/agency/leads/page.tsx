import AgencyPlaceholder from "@/components/agency/AgencyPlaceholder";

export default function LeadsPage() {
  return <AgencyPlaceholder title="Leads" subtitle="Prospects to close — pipeline from outreach to signed." note="Lead pipeline with upfront + recurring revenue totals." />;
}
